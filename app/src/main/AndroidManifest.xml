<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyectosqlite">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoSQLite">
        <activity android:name=".MenuPrincipal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ActivityAgregarCliente" />
        <activity android:name=".ActivityMostrarCliente" />
        <activity android:name=".ActivityEditarCliente" />
        <activity android:name=".MenuMantenedor" />
        <activity android:name=".MenuInforme" />
        <activity android:name=".DetalleItem" />
        <activity android:name=".Informes" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>